# Cloud automation (deprecated)

This project runs locally. Use:

- `./scripts/nightly-local.sh` — manual run
- `./scripts/install-schedule.sh` — 11pm launchd schedule
- `python site/serve.py` — browse reports

See [setup-remote.md](setup-remote.md) for full local setup.
